#include "main.h"
/**
 * _abs - computes the absolute value of an integar
 *
 * @i: input number of an integar
 * Return: Absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-1 * i);
	}
}

